# Workflow

- Prefers self-hosted, local-network deployment over relying on remote/third-party servers, explicitly valuing lower latency (chose to run a private LAN server for this reason). Confidence: 0.7
- Prefers to reverse-engineer and reimplement the server protocol while keeping the original client unmodified, over patching/modifying the client (selected the "reverse & reimplement" path). Confidence: 0.6
- Prefers an evidence-based, phased project approach for large uncertain work: verify claims/recon before committing, structure the work into phases with explicit go/no-go gates, and document findings in markdown docs as they emerge (chose this structure for the deadshot.io private-server project and produced docs/protocol.md + README; when the user asserted a DevTools artifact was "the file with full game logic", they asked the assistant to check/verify it against the repo rather than accept it). Confidence: 0.6
- Engages in reverse-engineering workflows — deobfuscation, VM sandboxing with mocked browser APIs, decoder table extraction, network endpoint discovery — and expects the assistant to use Node scripts for these tasks rather than inline shell one-liners (inline `node -e` one-liners proved unreliable — syntax errors and multi-request probes failed silently — so file-based scripts under tools/ are preferred). Confidence: 0.8
- When the user reports a change in external conditions (e.g., "network changed"), they expect previously-blocked work to be re-verified immediately rather than assumed still blocked. Confidence: 0.5
- The user does hands-on captures on their own machine (e.g., saving a DevTools `VM###` script to `VM9.txt`) and drops the raw artifact into the workspace referencing it by filename (@-mention), expecting the assistant to locate, read, and analyze it without further prompting. Confidence: 0.6
- Prefers attempting the deeper/harder goal (e.g., fully decrypting the encrypted WASM to understand the protocol codec) before settling for a pragmatic workaround (letting the client decrypt natively and observing frames via the local-server fallback); when the assistant proposed skipping straight to the server skeleton, the user re-directed with "try final.pkg decryption". Confidence: 0.45
- Prefers cutting project scope down to the essential feature rather than building full system parity — explicitly redirected "skip matchmaking we are only doing private rooms/join" when the assistant was about to implement the matchmaking flow, keeping the server focused on direct private-room/join support only. Confidence: 0.7
- When analyzing protocol trust boundaries or client/server behavior, the user wants the analysis to target the real production server's behavior (grounded in live captures and the real client bundle), not the local reimplementation/skeleton — re-directed the assistant with "i am not talking about the local server, but the real online server of deadshot" when it started from the local server code. Confidence: 0.7
- Wants the trust/exploit analysis to extend past auth and matchmaking into gameplay data too — explicitly asked for "more of gameplay, like location and recoil, ammo and other data", i.e. interested in which gameplay values are client-asserted vs server-authoritative (input bitmasks, aim angles, shot hit points, ammo, weapon stats) and which are spoofable; re-affirmed by successive deep-dives into enemy status sync and server-authoritative bullet hit logic (damage tables, angle tolerance, headshot pitch checks). Confidence: 0.75
- Prefers an empirical visual-testing loop for protocol analysis: build a focused test tool (e.g., a browser extension), deploy it against the live production game, observe the visual/behavioral result, and report findings back to inform the next step — skeptical of paper-only analysis and wants real-world verification. Confidence: 0.5
- Prefers terse, high-trust directives and autonomous bulk execution — issues minimal commands like "fix all", "how games like free fire and pubg has spectate mode , same for debug", single-word approvals like "yes" to a proposed multi-step patch, or dropping a bare shellout log path on crash ("tehse server crashed /tmp/...log") expecting the assistant to read the log, diagnose, patch, syntax-check and restart autonomously without asking for prioritization or step-by-step confirmation. Confidence: 0.82
- Requires the deadshot private server to always run with `DS_LAN_MODE=1` (`DS_LAN_MODE=1 node server/src/index.mjs` listening on 8080/8081/8082) to force the local WebSocket path via `Gq=!![]` injection; the stock `Gq` check only treats `127.0.0.1`/`localhost`/`192.168.*` as local, so `10.x` LAN IPs (e.g., `10.226.112.224`) otherwise take the remote `wss://ip_*.deadshot.io` path which fails DNS — explicitly re-requested as "restart the server on lan mode" after this root cause was diagnosed and now expects plain "restart the server" to mean restart in LAN mode with kill → restart → verify listening. Confidence: 0.85
- Prefers validating LAN party/multiplayer flows with two concurrent real browser clients both loaded with deadshot (explicitly requested "launch two instances of browser with ... deadshot loaded") rather than single-client or mocked-client checks, using the dedicated two-browser e2e harness to verify party join, spawn and movement sync. Confidence: 0.65
- Prefers to check and reuse existing real-server captures before launching fresh GPU-accelerated instances — explicitly directs to inventory/analyze already-captured real-server data for enemy replication (how the server sends info about the enemy) before spinning up new captures. Confidence: 0.78
- Prefers dual-client captures against the live production server (https://deadshot.io) with strictly isolated per-client wire logs — explicitly requested to "record both client data and their server data separately" for two simultaneous instances so the same match can be cross-validated from two viewpoints (each client's S vs R frames saved to separate artifacts like real-spawn-clientA/B.json). Confidence: 0.85
- Expects a concrete written plan before building multi-client capture tooling — prompted "what the plan" before launching two real-server instances and reviewed Chrome launch, CDP Network.webSocketFrame wiring, and per-client file outputs in a plan document. Confidence: 0.70
- Prefers per-client wire captures to retain both directions (client→server and server→client) so movement can be correlated to replication — explicitly confirmed "recording client info also" to check "on what movement of enemy the server sends us what data," expecting each artifact to contain both S frames (e.g., msg 1 inputs/shots) and R frames (e.g., msg 2 K11Co2hvi1l position/yaw/pitch/hp) separately rather than server-only logs. Confidence: 0.78
- Prefers assistant to directly launch and run capture/automation tooling while user handles manual interactive gameplay — explicitly directed "run it youreslf i will play" expecting assistant to execute the duo recorder (VISIBLE=1, port/display checks) without handing commands back for manual execution. Confidence: 0.72
- Prefers wire-level parity validation between the real production server and the local reimplementation for the same client inputs across all gameplay systems — explicitly requested for enemy movements ("how does server handels the enemy movements and for same movements what does our local server output") expecting a side-by-side diff of replication (msg 2 position/yaw/pitch/anim/hp/tick/qXu, batch rate and tick delay), and for combat flow ("other things that real server does ( bullet hit and kill) does our server does it properly") expecting comparison of the shot → impact/blood → damage → hitmarker → death → killfeed/score sequence (msgs 8/9/10/13/20/24/25/31). Confidence: 0.70
- Prefers local server hit detection to mirror the real server's client-trusted model: when shot 8 carries AHP/mGO/MHn hitPoint, validate by proximity to target body/head (body radius ~0.9, head radius ~0.32) with line-of-sight check and score by distance, falling back to strict yaw/pitch raycast only when hitPoint absent — explicitly approved with "yes" to switching from strict angle-tolerance to trusted hitPoint to fix unhittable cases, including correcting killfeed KiQ to headshot?1:0 and hitmarker wtZ to headshot?0:1. Confidence: 0.82
