# Tooling

- Prefers Node.js + TypeScript for server-side projects; when offered Go/Rust/Python alternatives for the game server, chose Node.js + TypeScript (same language as the JS client, so protocol codecs port directly). Confidence: 0.7
- Runs on Windows and is willing to disable Windows Defender when it false-positives on obfuscated/packed JS during reverse-engineering work (turned it off to unblock analysis after Defender quarantined the obfuscated client file). Confidence: 0.6
- Uses Chrome browser extensions (Manifest V3, content scripts with `all_frames: true` and `match_origin_as_fallback: true`) for runtime WebSocket interception and protocol testing of browser games, rather than proxy tools or client-patching approaches. Confidence: 0.5
- Prefers hardware-accelerated (GPU) visible, interactively usable browser instances when launching deadshot for testing/local play, explicitly requesting GPU acceleration and correcting headless/background launches to get controllable windows ("no launch them so that i can use them"). Confidence: 0.8
- Prefers a single hardware-accelerated Electron window that co-hosts the game and server debug side-by-side in a split pane (WebContentsView ~62% game at `/` + ~38% debug at `/debug`) over separate windows/tabs for inspection, with GPU compositing flags (`ignore-gpu-blocklist`, `enable-gpu-rasterization`, `enable-zero-copy`) and `setBackgroundThrottling(false)` enabled so both WebGL contexts stay GPU-composited, and `F9` toggle for the debug pane. Confidence: 0.7
- Prefers server debug view to enumerate all currently active rooms and allow inspecting any room from any player's perspective (per-room player list with POV/chase spectating) rather than a single-room or aggregate-only view. Confidence: 0.85
- Prefers debug spectate window to reuse the game's native spectate-mode rendering rather than building a custom Three.js map/scene reconstruction; explicitly rejected custom map creation and requested PUBG/Free Fire parity ("do not create you own map, the debug window should show how games show spectate mode", "how games like free fire and pubg has spectate mode , same for debug"). Confidence: 0.92
- Prefers debugger's Draco decoder to load via WASM (type: 'wasm' with preload and application/wasm MIME, JS fallback on error) for compressed map/character meshes rather than JS-only decoding ("so the debugger must be loading wasm file"). Confidence: 0.85
- Prefers debug tooling to live client-side as the production client augmented with extra debugging features rather than a separate server-side implementation, explicitly correcting placement with "well the debug should be client" / "client with extra features" — expects debug UI/code under client/ not server/. Confidence: 0.85
